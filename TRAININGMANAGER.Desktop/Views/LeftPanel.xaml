<UserControl x:Class="TRAININGMANAGER.Desktop.Views.LeftPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TRAININGMANAGER.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <Border CornerRadius="10,0,0,10" Grid.Column="0">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource backgroundSecondaryColor1}" Offset="0"/>
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel Orientation="Vertical"                     
                    Margin="0,0,0,0">
            <!-- Logo -->
            <Image Margin="0 30 0 0" Source="/Images/cim_logo.png" Height="40"/>
            <!-- Menu buttons -->
            <local:Menu Margin="0 20 0 0"/>
        </StackPanel>
    </Border>
</UserControl>
